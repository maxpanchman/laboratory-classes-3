<!DOCTYPE html>
<html>

<head>
    <title>
        <%= headTitle %>
    </title>
    <link rel="stylesheet" href="/css/main.css">
</head>

<body>
    <%- include('partials/navigation', { menuLinks: menuLinks, activeLinkPath: activeLinkPath }) %>
        <main>
            <h1 class="heading">Newest product</h1>
            <% if (newestProduct && Object.keys(newestProduct).length > 0) { %>
                <div>New product data -
                    <%= JSON.stringify(newestProduct) %>
                </div>
                <% } else { %>
                    <div>No new products available.</div>
                    <% } %>
        </main>
</body>

</html>